# Changelog

A running log of completed work. Append a brief bullet at the top of each session. No need for categorized sections — keep it simple and in descending chronological order (latest first).

---

- **[2026-08-24]** Enhanced box drag multiselection for faces in `LocalGeometryOverlay` (`EditMode::Face`): upgraded candidate detection from centroid-only to full edge-crossing (`SegmentIntersectsBox2D`), any-part overlap, and point-in-polygon testing. Resolved inverted screen projection bug by enforcing strict camera near-plane forward culling ($D > 0.1\text{m}$) across all face vertices, preventing geometry behind the camera from projecting into the marquee. Fixed raycast occlusion test against multi-triangle occluders and deferred single-click toggle picking to mouse release on stationary clicks ($\le 5\text{px}$ delta) to prevent drag-selection start from deselecting faces.
- **[2026-08-24]** Deprecated the linear integer palette slider in `TextureMapPanel` and implemented Option 2: a compact stepper button pair (`[◀]` / `[▶]`) with cyclic wrap-around navigation between 0 and $N-1$ combined with an enumerated dropdown combo box displaying full-width spanning 16-color CLUT bars across each option item, handling single-palette textures with clean UI disabling and paired with a full-width real-time interactive color inspector preview bar. Fixed UV coordinate scaling and 2D canvas preview sizing for half-page (`H`-suffixed) textures (128×256 px in 4bpp mode). Resolved horizontal compression/stretching in the 3D viewports, `TextureMapPanel` tile dimension reporting, and `PLMWrite` binary export by normalizing and denormalizing UV coordinates relative to the actual texture width (`tw = 128.0f` for `H`, `tw = 256.0f` for `F`/default). Unified 2D texture edit canvas scaling in `TextureEditPanel` against the standard 256×256 reference scale so half-width textures maintain consistent vertical height and pixel grid proportions without double-height scaling. Verified zero-loss roundtrip across all 15,883 geometry faces in the dataset.
- **[2026-08-24]** Renamed viewport source and header files in `src/viewport/` and `include/viewport/` to match their declared class names (`CollisionOverlay`, `GlobalGeometryViewport`, `LocalGeometryOverlay`, `SceneOverlay`, `ViewportSync`, `WaypointsOverlay`), establishing complete 1:1 filename-to-class alignment across the viewport module. Updated all `#include` directives across the codebase, `ARCHITECTURE.md`, and format documentation.
- **[2026-08-24]** Renamed all UI panel source and header files in `src/panels/` and `include/panels/` to append `Panel` to their filenames (`ChunksPanel`, `DependenciesPanel`, `LocalGeometryPanel`, `MapsPanel`, `MenuPanel`, `OutlinerPanel`, `SettingsPanel`, `TextureEditPanel`, `TextureMapPanel`, `ViewportToolsPanel`, `WaypointsPanel`), establishing 1:1 alignment with their class/namespace names and eliminating filename collisions with `include/viewport/`. Updated all `#include` directives across the codebase, `ARCHITECTURE.md`, and task tracking documents.
- **[2026-08-23]** Fixed 3D viewport transparency depth-blocking by introducing an alpha-cutout GLSL shader (`alpha_discard.fs` with embedded fallback) in `TextureCache` that calls `discard` on transparent fragments (`color.a < 0.1`). Updated `CreateMeshMaterial()` across all viewport batch builders and fixed `GpuBatch` resource deallocation to safely preserve shared shaders and cached textures.
- **[2026-08-23]** Implemented IPDCreate and PLMCreate utilities for blank chunk generation. Fully decoupled TIM processing logic from Raylib core, establishing TIMDecoder and TIMEncoder modules in the formats namespace.
- **[2026-08-23]** Documented the Silent Hill 1 rendering engine architecture and file responsibilities in `docs/Game Rendering.md`, detailing low-level GTE rasterization (`bodyprog_80055028.c`), IPD chunk terrain and subcell PVS culling (`bodyprog_80040B74.c`), world scene dispatch (`world_draw.c`), skeletal hierarchies (`bodyprog_bone_80044F14.c`), animation playback (`bodyprog_anim_800445A4.c`), environment/flashlight effects (`map_effects.c`), and brightness calibration (`option_brightness_line.c`).
- **[2026-08-15]** Decoupled all live geometry operations (`FaceOperations`, `VertexOperations`, `MeshOperations`, `GlobalObjectOperations`, `SubdivideFace`) and `History::ApplyMesh` (Undo/Redo) from automatic disk saving (`IPDWrite::WriteChunk`). All edits and undo/redo now mutate in-memory chunk data and GPU batches exclusively in RAM, restricting disk writes to explicit user save commands (`Ctrl+S` / `Ctrl+Shift+S` / `File -> Save`).
- **[2026-08-15]** Updated Face Mode extrusion in `ExtrudeFaces` (`FaceOperations.cpp`): binds extrusion distance directly to the active Grid Snap slider value, replaces the base face with the extruded cap face while preserving base vertices, creates outward-facing side walls inheriting texture and CLUT properties with tile UV bounds, maintains exact texture rotation and mirroring on the cap face, and auto-updates selection to the newly extruded cap face(s).
- **[2026-08-15]** Enhanced 4-vertex extrusion in `ExtrudeSelectedVertices` (`VertexOperations.cpp`): when 4 selected vertices form a face, extrudes along the face normal creating 5 outward-facing quad faces (4 walls + 1 cap), inheriting texture, CLUT palette, and tile UV coordinates while maintaining exact rotation and mirroring on the cap face, removes the original base face, and automatically updates the active selection to the new cap face vertices.
- **[2026-08-15]** Fixed crash in `ExtrudeSelectedVertices` (`VertexOperations.cpp`): bridge quad faces were being pushed to `mesh.faces` inside a range-for loop over the same vector, causing iterator invalidation on reallocation. Staged bridge faces in a separate `bridgeFaces` vector and appended after the iteration loop completes.
- **[2026-08-15]** Fixed `IPDWrite::WriteChunk` binary corruption on prop duplication by updating `ipdDataOffset` back into live `RenderObject` memory and cascading shifted offsets. Implemented dedicated Face and Vertex editing modules (`FaceOperations.h/cpp`, `VertexOperations.h/cpp`), wiring up all panel tool buttons and viewport context menus for quad triangulation, triangle bridging, normal-aligned face extrusion (Connect & Separate modes), winding inversion, face/vertex deletion with isolated vertex pruning, UV texturing (tile painting, 90° UV rotations, UV flipping, fit/reset bounds), vertex grid & raycast floor snapping, planarization along X/Y/Z, face creation from 3-4 points with angle sorting, vertex extrusion, and tolerance-based vertex welding. Updated `ARCHITECTURE.md`.
- **[2026-08-15]** Enhanced Snap to Floor with downward triangle raycasting against underlying ground polygon faces across chunks, integrated `History` undo/redo directly into disk save write-backs (`IPDWrite::WriteChunk`), resolved workspace path resolution across execution working directories, and ensured duplicated global objects allocate clean new `IPD_OBJ_DATA` index entries without overwriting existing placements.
- **[2026-08-15]** Implemented functional Global Prop and Mesh operation modules (`GlobalObjectOperations.h/cpp`, `MeshOperations.h/cpp`), linking panel tool buttons and viewport right-click context menus to functional 90° yaw/axis rotations, floor/grid snapping, prop & mesh duplication, deletion, separation, merging, pivot centering, axis mirroring with face winding inversion, and low-poly PS1 primitive generation. Relocated `ChunkValidator` from `core/` to `geometry/`.
- **[2026-08-15]** Expanded Local Geometry into 4 discrete editing modes (`Global Objects`, `Meshes`, `Faces`, `Vertices`), separating `_GLB.PLM` prop positioning from local mesh management, and configured dynamic mode-specific tool dispatch across the frontend.
- **[2026-08-15]** Decoupled `ViewportToolsPanel` into a pure conduit container and created `LocalGeometryPanel` in `panels/` featuring a Blender-inspired toolset (mode selector, topology operations, transforms, snapping, Crocotile UV tools, primitives creation, and PS1 limits validation).
- **[2026-08-15]** Refactored codebase file and class names to establish 1:1 filename-to-class alignment across core, panels, viewport, and geometry modules (`FileDialog`, `OverlayLoader`, `History`, `Textures`, `ChunksPanel`, `OutlinerPanel`, `TextureEditPanel`, `TextureMapPanel`, `WaypointsPanel`, `SceneOverlay`, `WaypointsOverlay`) and updated `ARCHITECTURE.md`.
- **[2026-08-09]** Implemented hierarchical view-frustum culling in `EditViewport` ("Local Geometry") using a custom `Frustum` utility class (`Frustum.h`). Culls off-screen chunk and object bounding boxes in $O(1)$ time, skipping thousands of unneeded `DrawLine3D` wireframe calls, face highlights, and vertex hover/selection markers outside the camera view.
- **[2026-08-09]** Implemented deferred 3D viewport render target resizing in `Viewport3DBase`. Render texture reallocations now defer while dragging ImGui dock splitters (`ImGui::IsMouseDown(ImGuiMouseButton_Left)`), dynamically scaling the texture with unified mouse coordinate mapping for 100% pixel-accurate ray picking/box selection, and reallocating OpenGL FBO textures once upon mouse release to eliminate GPU allocation churn, CPU lag, and console log spam.
- **[2026-08-09]** Reworked the undo/redo buffer in `EditHistory` to automatically combine repeated actions and flatten successive changes targeting the exact same sub-element/value into single before/after snapshots across all viewports and panels. Added dynamic target signature matching (`GetMeshTargetSignature` / `GetOverlayTargetSignature`) so operations on different faces, vertices, waypoints, or links remain separate undo steps while continuous edits on the same target flatten automatically. Discards zero-net-change action sequences.
- **[2026-08-09]** Overhauled door and waypoint undo tracking in `LinkageInspector` (`TrackWidget`). Fixed ImGui pre-mutation state capturing so `before` overlay state is captured prior to widget mutation and pushed on edit completion (`IsItemDeactivatedAfterEdit`). Added dynamic `Undo [Action]` and `Redo [Action]` items to the top `Edit` menu bar, fully integrating all waypoint and link field edits (target waypoint, SysState, destination map, trigger type, activation type, params, flags, and buttons) into `Ctrl+Z` / `Ctrl+Y`.
- **[2026-08-09]** Relocated map overlay binary update/deployment buttons (**Save Overlay JSON** and **Deploy to Decomp C Source**) from 'Tools' to the 'Maps' panel. Organized 'Maps' into collapsible `Actions` and `Maps` containers, removed redundant active map label and status column, and highlighted modified map table rows in orange. Streamlined Engine file table size bounds indicators in 'Chunks'.
- **[2026-08-09]** Implemented SHA256 checksum-based diff logic for both chunk saving (`IpdWriter`) and deployment (`deploy_workspace.py`). Scans across all chunk file types (`.IPD`, `.PLM`, `.TIM`, `.BIN`) and indexes original assets across all subdirectories to ensure only modified files are saved or deployed, keeping console output discreet.
- **[2026-08-09]** Fixed viewport chunk grid misplacement by updating `DrawCustomGrid()` in `Viewport3DBase` to align with `ChunkManager`'s 16x18 dataset range (X: $[-8, 7] \to [-320, 320]$, Z: $[-8, 9] \to [-400, 320]$). Added polymorphic `virtual void DrawViewportGrid()` hook to `Viewport3DBase` so `GlobalGeometryViewport` cleanly overrides grid drawing to display only its isolated $40 \times 40$ centered grid without double-rendering the full chunk map grid.
- **[2026-08-08]** Implemented 100% reversible round-trip overlay repacker (`pack_overlay_to_decomp.py`). Fixed `TriggerType_EndOfArray` sentinel preservation in `extract_overlay.py` and `pack_overlay_to_decomp.py`, restoring exact array element counts (`MAP_EVENTS[72]`). Fixed missing `.triggerType` fields by ensuring explicit emission of `.triggerType = TriggerType_None` across all structs. Implemented robust Q12 decimal snapping algorithm in `format_q12` (eliminating `5.1001f` rounding artifacts to clean `5.1f`), stripped outer array variable wrappers, and removed loose trailing commas from `map_points.h`. Configured `git rm --cached` and `.gitignore` for `src/maps/**/map_points.h` and `src/maps/**/*_events_data.c` so generated overlay files appear as deleted (`D`) in git commits instead of modified (`M`), while physically persisting on disk for compilation. Configured **Deploy to Decomp C Source** in `Tools` to target only the active selected map, and documented Strategy B binary ROM layouts in `docs/formats/binary_overlay_rom_packing.md`.
- **[2026-08-08]** Implemented `GetChunkLocations()` override in `EventViewport` ("Doors and Waypoints") to correctly render 3D chunk boundary boxes, translucent volumes, and 2D billboard labels. Bound `TAB` key as a hold-to-view shortcut across viewports (legend displays while `TAB` is held down, hides when released).
- **[2026-08-08]** Enhanced Room Linkage Editor visualization in C++ Editor. Created header `MapTable.h` using 100% decomp-verified map metadata, implemented the `Maps` panel docked on the left panel group alongside `Chunks`, resolved relative executable directory overlay JSON pathing, aligned Z-axis coordinates ($Z \to -Z$), and added 3D thick relationship beams connecting doors across maps with a `Show All Door Relationships` checkbox in the `Tools` panel.
- **[2026-08-08]** Optimized GPU geometry loading/unloading in C++ Editor. Silenced Raylib trace log I/O overhead via `SetTraceLogLevel(LOG_WARNING)` in `main.cpp`, refactored `EditViewport` to share GPU geometry arrays from `ViewViewport` (50% reduction in VAO/VBO allocations), and updated `ViewportSync` to upload geometry once per chunk.
- **[2026-08-08]** Fixed `ToolPanelWindow` panel name routing and `TextureManagerWindow` ("Textures") TIM texture loading. Fallback chunk selection to loaded viewport chunks when 2D grid selection is empty, and stripped `.TIM` extensions from `IpdInspector` texture list options to prevent double-extension pathing (`.TIM.TIM`) when assigning textures.
- **[2026-08-08]** Implemented `room_linkage_editor` task for `unified_cpp_editor`. Built `extract_overlay.py` extracting 2,123 waypoints and 2,234 events across all 43 maps to `data/workspace/overlays/`. Built C++ `OverlayLoader` for JSON load/save, created `EventViewport` ("Doors and Waypoints") rendering color-coded arrival waypoints, precise trigger volumes (AABB, TouchFacing circle, OBB rectangle), and direction lines with zero extra VRAM, integrated linkage inspector controls directly into the dynamic `Tools` (`ToolPanelWindow`) panel with map selector dropdown (`MAP0_S00` to `MAP7_S03`), SysStates, and **Link Bidirectionally** cross-overlay synchronization.
- **[2026-08-06]** Implemented `ChunkValidator` system (`ChunkValidator.h/cpp`) enforcing $16.0\text{f}$ height limits ($16 \ll 8 = 4096$ raw units), chunk cell bounds with $7$ raw units overhang, and vertex capacity. Integrated validation UI into `ToolPanelWindow` with clickable navigation to offending geometry, enabled auto-validation in `EditViewport`, and added stubs for dynamic chunk mesh ownership determination (`DetermineChunkOwnerForMesh`).
- **[2026-08-06]** Fixed EditViewport `SubdivideSelectedFaces` geometry garbling on vertical faces, made face subdivision 100% deterministic, enforced strict PS1 255 vertex-per-mesh limit with console warnings, interpolated raw UV coordinates for accurate IPD binary exports, and expanded `SceneOutliner` with per-mesh vertex budget state and limit warnings.
- **[2026-08-06]** Investigated and fixed missing road components and global PLM geometry in `THRFBFE`, `THRFDFC`, and all negative/hex coordinate chunks by resolving a chunk prefix derivation bug (`DeriveChunkPrefix`), restoring full resolution of global shared PLMs (such as `THR_GLB.PLM`) across `unified_cpp_editor` C++ modules and Python pipeline tools. Updated 3D Viewport Legend to vertically visualize the maximum theoretical chunk bounds (`-16.0f` to `16.0f` world units), while keeping XZ dimensions locked to the 40x40m grid cell boundaries, and added a `Reset Camera` button to the status bar immediately to the right of `Controls`.
- **[2026-08-04]** Overhauled asynchronous architecture in C++ Editor. Implemented background threading for chunk geometry loading, texture decoding, and disk IO to prevent main-thread rendering stalls. Added smooth ImGui progress-bar modal overlays to track asynchronous scene loading and heavy offline Python asset pipeline tasks (`ExtractToWorkspace`, `DeployToTarget`, etc.) in real-time.
- **[2026-08-04]** Fixed critical C++ Editor stability and performance bugs. Implemented a spatial acceleration broad-phase culling system for vertex box-picking in `EditViewport` reducing occlusion raycast time from O(V * F) to O(1) lookups. Resolved C-struct shallow copy GPU memory leaks on `std::vector` reallocations using strict C++ RAII move-semantics and destructors for `Mesh` encapsulation. Fixed quad fan winding order hardware-flag loss during runtime IPD subdivides.
- **[2026-08-03]** Implemented Direct Binary Editing in C++ Editor. Created `EditViewport` with Raycast picking for Objects, Faces, and Vertices. Added real-time structural editing capabilities including multi-face subdividing into 1.0 unit grid tiles. Integrated a robust global `EditHistory` stack for Undo/Redo across discrete mesh snapshots. Expanded the `TextureManager` to apply real-time texture IDs, UV drags, and CLUT swaps seamlessly to multiple faces simultaneously using `Control`+click logic, auto-syncing GPU batch rebuilds. Introduced `IpdWriter` for fully native PS1 binary repacking of chunks and layout modifications directly back to the active workspace.
- **[2026-07-31]** Improved C++ Editor Collision Viewport. Resolved material/color bugs. Categorized and color-coded collision walls (`PHYSICAL`, `CAMERA`, `VOID`) and fixed downward extrusion logic to anchor at floor `Y=0` with minimum height clamps. Implemented explicit 3D line rendering for floor/sound subcell triggers using deterministic colors derived from `tilt_flags`. Added fully decoupled 2D Orthographic projections (Top/Front/Left/Right/Back) to the base viewport with Blender-style numpad hotkeys, MMB orbiting, Shift+MMB panning, and a toggle for visual geometry overlays.
- **[2026-07-30]** Overhauled Python pipeline architecture into a modular `sh1_core` library, removing monolithic script redundancies and unifying CLI wrappers. Re-integrated Python pipeline cleanly into the C++ Editor (`AssetPipeline.cpp`), eliminating brittle native C++ struct serialization in favor of stable `std::system` orchestration, and fixed project directory script path resolution.
- **[2026-07-30]** Refined C++ Editor Viewport projection, replacing global window coordinate scaling with local viewport `GetWorldToScreenEx` mapping. Fixed true `16x18` dataset boundaries for the `ChunkManager` grid and 3D viewport. Optimized chunk offloading vectors to `O(1)` swap-and-pop, separated horizontal/vertical camera translation scalars for granular alignment, and overhauled Chunk Manager viewport-active highlights to a clean white inset-border design.
- **[2026-07-30]** Extended C++ Editor 3D Viewports. Implemented `CollisionViewport` for isolated collision mesh rendering. Added shared `ViewportCameraState` for synchronized flying (WASD/Space/Shift) between visual and collision viewports. Added a customizable 3D coordinate grid with dynamic major/minor subdivisions, colored floor legends per chunk, and 3D billboard text labels. Fixed 2D `ChunkManager` axis offsets and color alignments to precisely match the 3D chunk boundary definitions.
- **[2026-07-27]** Implemented Phase 5 — 3D Viewport. Built `IpdParser` (full IPD+PLM geometry extractor with faithful coordinate math, UV bias, CBA palette decode, quad winding, and global PLM support). Added `TextureCache` singleton for GPU palette textures (one `Texture2D` per `(texName, paletteRow)`). Created `Viewport3D` (dynamic Raylib `RenderTexture2D`, orbital camera gated by ImGui hover, `DrawMesh` batching). Added `SceneOutliner` collection-tree panel. Wired "Load to Viewport" from `ChunkManager` selection.
- **[2026-07-27]** Ported `3_ipd_pipeline` Python GUI into the C++ `ChunkManager`. Implemented interactive 2D map grid with dark mode aesthetics, dictionary aliasing, automatic configuration saving, and a native C++ `EnginePatcher` to rewrite memory allocations in `filetable.c.*.inc` without Python subprocesses.
- **[2026-07-27]** Initialized Unified C++ Editor (ImGui/Raylib). Ported IPD binary structures, implemented C++ TIM parser to VRAM, built native Windows file dialogs, and created 2D Chunk Manager hierarchy viewer.
- **[2026-07-25]** Documentation overhaul: restructured root files, introduced `tasks/` system, renamed and reorganised `docs/` and `docs/formats/`, rewrote `TOOLING_MANUAL.md` and `SESSION_GUIDANCE.md`.
- **[2026-07-25]** Resolved Blender UV roundtrip bugs (tex_num bitwise masking, UV clamping off-by-one). Implemented tile paint mode in Blender addon with real-time 32×32 tile preview.
- **[2026-07-22]** Built standalone RAM & VRAM visualizer for the PC port (`ram_visualizer.c`), drawing loaded chunks and textures live via SDL.
- **[2026-07-17]** Fixed critical collision pointer bug in `local_json_to_ipd.py` (offsets were absolute instead of `+0x54` relative). Fixed Blender context errors in import/export operators.
- **[2026-07-17]** Built full Blender bridge (`tooling/4_blender_bridge/`): bidirectional JSON↔Blender importer/exporter, custom IPD properties on meshes, pixel-accurate UV and material generation.
- **[2026-07-13]** Created Tkinter GUI (`tooling/3_ipd_pipeline/gui.py`) for the forward/backward pipeline. Demonstrated successful position editing of individual objects via JSON.
- **[2026-07-11]** Created `tooling/aseprite/clut_switcher.lua` for real-time CLUT palette swapping in Aseprite.
- **[2026-07-11]** Introduced dual-PNG architecture (`tim_to_png.py`, `png_to_tim.py`) replacing lossy TGA. Built `build_map_assets.py` (map-specific GLB JSON). Completed lossless round-trip for 21 THR chunks and 10 TIM textures. Verified `local_json_to_ipd.py` backwards converter.
- **[2026-07-10]** Built `create_data_samples.py` for isolated sample generation. Reworked pipeline to JSON-only intermediates, removed `.bin` dependencies.
- **[2026-07-09]** Created `plm_dump.py`, initial `FACTS.md`, `HYPOTHESES.md`, `ROADMAP.md`. Established project structure.
- **[2026-07-09]** Developed `ipd_to_obj.py` — first functional Python IPD→OBJ converter. Validated geometry, normals, UVs, and TGA texture extraction against reference C tool.
